<h1>フォロワー一覧</h1>

<% if @users.empty? %>
  <p>フォロワーはいません。</p>
<% else %>
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to user.nickname, user_path(user) %></li>
    <% end %>
  </ul>
<% end %>
